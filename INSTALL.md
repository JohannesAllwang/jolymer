## Expert

The library requires python 3.11 or higher and can be installed with pip:

```
pip install -e .
```

## Beginner

Scripts are available for Windows installation:

## Windows

We will use powershell and pyenv:

### Pyenv-win

Install with:

```
winget --id pyenv-win --scope user
```

Execute install script:

```powershell
/path/to/jolymer/scripts/build_nuitka.ps1
```
